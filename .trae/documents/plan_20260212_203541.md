I will migrate the `goal-tracker` MCP server to the Code Mode architecture using the `mcp-code-architect` agent.

### 1. Analysis & Preparation
- Analyze `src/index.ts` to extract:
  - Data interfaces (`Goal`, `Milestone`, `GoalStatus`, etc.)
  - State management logic (in-memory storage?)
  - Tool implementation logic (`goalTracker` tool handlers)

### 2. Core Layer Implementation (`src/core/`)
- **`src/core/types.ts`**: Define shared interfaces.
- **`src/core/tracker.ts`**: Implement pure business logic (add, update, complete, list, check, remove) without MCP dependencies.

### 3. Code Mode Layer Implementation (`src/codemode/`)
- **`src/codemode/index.ts`**: Create a `GoalTracker` class that wraps the Core logic.
  - Expose strongly-typed methods corresponding to the tool operations.
  - Ensure clean API surface for programmatic use.

### 4. MCP Layer Implementation (`src/mcp/`)
- **`src/mcp/tools.ts`**: Define the `goalTracker` tool schema using the extracted types.
- **`src/mcp/server.ts`**: Implement the MCP server setup.
  - Map the `goalTracker` tool to `GoalTracker` class methods.
  - Handle argument parsing and error mapping.

### 5. Integration & Entry Point
- **`src/index.ts`**:
  - Export the `GoalTracker` class from `src/codemode`.
  - Conditionally run the MCP server (if main module).

### 6. Testing & Validation
- Refactor `src/index.test.ts` to test the `GoalTracker` class directly (Code Mode testing).
- Add integration tests for the MCP layer if needed.
- Verify backward compatibility of the `goalTracker` tool.

### 7. Documentation
- Update `README.md` to reflect the new architecture.
- Document the new Code Mode API usage.
