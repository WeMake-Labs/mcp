{
  "name": "@wemake.cx/metacognitive-monitoring",
  "version": "0.1.5",
  "description": "Metacognitive Monitoring MCP server",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/WeMake-AI/mcp.git",
    "directory": "src/metacognitive-monitoring"
  },
  "publishConfig": {
    "access": "public"
  },
  "homepage": "https://github.com/WeMake-AI/mcp/tree/main/src/metacognitive-monitoring",
  "author": "heyFlorentin",
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=18.0.0"
  },
  "packageManager": "bun@1.2.0",
  "keywords": [
    "model-context-protocol",
    "metacognitive-monitoring"
  ],
  "bin": {
    "mcp-server-metacognitive-monitoring": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build --entrypoints ./src/index.ts --outdir ./dist --format esm --target=bun --sourcemap && (if ! head -1 dist/index.js | grep -q '^#!'; then echo '#!/usr/bin/env bun' | cat - dist/index.js > dist/index.tmp && mv dist/index.tmp dist/index.js; fi) && chmod +x dist/*.js",
    "types": "bun tsc",
    "prepare": "bun run types && bun run build",
    "test": "bun test",
    "test:ci": "bun test --reporter=junit --reporter-outfile=test-results.xml --coverage",
    "release": "bun publish --tag latest",
    "watch": "bun tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4"
  },
  "devDependencies": {
    "@types/node": "^22.17.2",
    "typescript": "^5.9.2"
  }
}
