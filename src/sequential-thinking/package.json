{
  "name": "@wemake-ai/mcp-server-sequential-thinking",
  "version": "1.0.0",
  "description": "MCP server for sequential thinking and problem solving",
  "license": "MIT",
  "author": "WeMake (https://wemake.cx)",
  "homepage": "https://wemake.cx",
  "bugs": "https://github.com/WeMake-ai/mcp/issues",
  "type": "module",
  "bin": {
    "mcp-server-sequential-thinking": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build --target=node --outfile=dist/index.js index.ts && bun tsc && shx chmod +x dist/*.js",
    "prepare": "bun run build",
    "watch": "tsc --watch",
    "publish": "bun publish --access public"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.0",
    "chalk": "^5.4.1",
    "yargs": "^18.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "@types/yargs": "^17.0.33",
    "shx": "^0.4.0",
    "typescript": "^5.8.3"
  }
}
