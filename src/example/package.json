{
  "name": "@wemake.cx/example",
  "version": "0.0.1",
  "description": "MCP server for analogical reasoning: structured pattern mapping, domain-to-domain structure alignment, and explainable inference",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "https://github.com/wemake-ai/mcp.git",
    "directory": "src/example"
  },
  "publishConfig": {
    "access": "public",
    "tag": "example"
  },
  "homepage": "https://github.com/wemake-ai/mcp/tree/main/src/example",
  "author": "heyFlorentin",
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=18.0.0"
  },
  "packageManager": "bun@1.2.0",
  "keywords": [
    "model-context-protocol",
    "example"
  ],
  "bin": {
    "mcp-server-example": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "bun build --entrypoints ./index.ts --outdir ./dist --format esm --target=bun --sourcemap && (if ! head -1 dist/index.js | grep -q '^#!'; then echo '#!/usr/bin/env bun' | cat - dist/index.js > dist/index.tmp && mv dist/index.tmp dist/index.js; fi) && chmod +x dist/*.js",
    "types": "bun tsc",
    "prepare": "bun run types && bun run build",
    "test": "bun test",
    "test:ci": "bun test --reporter=junit --reporter-outfile=test-results.xml --coverage",
    "release": "bun publish --tag latest",
    "watch": "bun tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.4"
  },
  "devDependencies": {
    "@types/node": "^22.17.2",
    "typescript": "^5.9.2"
  }
}
